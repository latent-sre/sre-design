{{ generated }}
# Runbook: {{ name | inline }}

> {{ banner | inline }}

**Trigger:** alert `{{ alert_ref | inline }}`{{ HB }}
**Related flow:** `{{ related_flow | inline }}`{{ HB }}
**Escalation:** {{ escalation | inline }}

## Symptoms
{% for s in symptoms %}- {{ s | inline }}
{% else %}- (none)
{% endfor %}

## Diagnosis
{% for d in diagnosis %}1. {{ d | inline }}
{% else %}1. (none)
{% endfor %}

## Remediation
{% for r in remediation %}1. {{ r | inline }}
{% else %}1. (none)
{% endfor %}
{%- if plain_steps %}

## What this flow does (plain English)

{% for line in plain_steps %}{{ line }}
{% endfor %}
{%- endif %}
{%- if excerpts %}

## The code this runbook points at

The exact lines the knowledge base cites, verbatim from the scanned commit (shown only
while the file still matches the citation hash). You do not need to change them at 3am —
they tell you what the service will and will not do on its own.

{% for x in excerpts %}**`{{ x.header | inline }}`**

{{ x.fence }}{{ x.lang }}
{{ x.code }}
{{ x.fence }}

{% endfor %}
{%- endif %}
{%- if flow_diagram %}

## Flow

```mermaid
{{ flow_diagram }}
```
{% endif %}
