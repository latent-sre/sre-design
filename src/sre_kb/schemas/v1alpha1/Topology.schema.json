{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://sre.kb/schemas/v1alpha1/Topology.schema.json",
  "title": "Topology",
  "description": "The app-centric dependency graph: services and the datastores/brokers/externals they bind or call, as nodes and typed edges. Estate runs merge several services into one shared topology.",
  "type": "object",
  "required": ["kind", "spec"],
  "properties": {
    "kind": { "const": "Topology" },
    "spec": {
      "type": "object",
      "additionalProperties": false,
      "required": ["nodes", "edges"],
      "properties": {
        "nodes": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["type", "name"],
            "properties": {
              "type": { "type": "string" },
              "name": { "type": "string" }
            }
          }
        },
        "edges": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["from", "to"],
            "properties": {
              "from": { "type": "string" },
              "to": { "type": "string" },
              "relation": { "type": "string" }
            }
          }
        },
        "pcfSpaces": { "type": "array" }
      }
    }
  }
}
