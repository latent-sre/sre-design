{
  "service": "service",
  "_doc": "Labeled extraction truth for the S5 eval harness — coverage #15 FeatureFlag (the common.feature_flags collector: config blocks + @ConditionalOnProperty + flag-SDK calls).",
  "artifacts": [
    {"kind": "FeatureFlag", "name": "features-beta-search"},
    {"kind": "FeatureFlag", "name": "kill-switch-payments"},
    {"kind": "FeatureFlag", "name": "new-checkout"},
    {"kind": "FeatureFlag", "name": "new-pricing"}
  ],
  "detectors": [
    "common.feature_flags"
  ]
}
